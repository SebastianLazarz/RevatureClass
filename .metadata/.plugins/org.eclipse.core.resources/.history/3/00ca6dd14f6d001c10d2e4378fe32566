package com.revature;

public class Q5 {

	public String subString (String str, int idx) {
		String subString = "";
		char[] strToChars = str.toCharArray();
		
		for (int i=0; i < idx; i++) {
			subString = subString + strToChars[i];
		}
		
		return subString;
	}
}
